# 响应式

### 简易版本响应式

::: code-group

<<<./code/04-01.js

<<<./code/04-02.js {4,6-12,22-25}

<<<./code/04-03.js {1,21-46,54-63}
:::

每一次的变化，都在代码高亮中

从基础的 响应式-实现对对象属性的读取，设置，能够执行一些副作用函数

到迭代成 将副作用函数抽离为公共的（非硬编码的），再到 解决 属性与 副作用函数的 `1-N` 的关联关系，避免我更新一个属性，所有的副作用都执行一次的问题。

### 分支切换

::: code-group
<<<./code/04-04.js
<<<./code/04-05.js
:::

### 实现只读

如果想一个对象为只读，可以如下实现; 有深只读，和浅只读； 深只读则所有属性不可修改，不可删除， 浅只读只有第一层不可删除和修改，深层对象能删除。

深只读，要将所有属性进行 `代理`否则，没发拦截

<<<./code/04-10.js

### toRef & toRefs

proxy 只能代理对象，无法代理普通类型，比如 Number, Boolean, String 等等。
