# Vercel & 云平台部署

Vercel 是目前前端部署体验最好的平台之一，支持 SPA、SSR、Edge Functions 等多种部署模式。

## 一、Vercel 基础部署

### 1.1 项目初始化

```bash
# 安装 Vercel CLI
npm install -g vercel

# 登录
vercel login

# 关联项目（首次）
vercel link

# 本地预览
vercel dev

# 部署到预览环境
vercel

# 部署到生产环境
vercel --prod
```

### 1.2 vercel.json 配置

```json
{
  "version": 2,
  "name": "my-project",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",

  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],

  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ],

  "rewrites": [
    {
      "source": "/old-path",
      "destination": "/new-path"
    }
  ],

  "redirects": [
    {
      "source": "/blog/:slug",
      "destination": "/posts/:slug",
      "permanent": true
    }
  ]
}
```

### 1.3 环境变量

```bash
# 通过 CLI 设置
vercel env add API_BASE_URL production
vercel env add DATABASE_URL production

# 或通过 Dashboard 配置
# Project Settings → Environment Variables
```

```yaml
# .github/workflows/vercel-deploy.yml
name: Vercel Deploy

on:
  push:
    branches: [main, develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Deploy to Preview
        if: github.ref != 'refs/heads/main'
        run: vercel --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Production
        if: github.ref == 'refs/heads/main'
        run: vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
```

## 二、Vercel Edge Functions

在 Vercel 上运行轻量后端逻辑，无需单独服务器。

```typescript
// api/hello.ts
import type { VercelRequest, VercelResponse } from '@vercel/node'

export default function handler(req: VercelRequest, res: VercelResponse) {
  res.json({ message: 'Hello from Edge', timestamp: Date.now() })
}
```

```typescript
// api/ai-chat.ts
import type { VercelRequest, VercelResponse } from '@vercel/node'

export const config = {
  maxDuration: 30, // 最大执行时间（秒）
}

export default async function handler(req: VercelRequest, res: VercelResponse) {
  if (req.method !== 'POST') {
    return res.status(405).json({ error: 'Method not allowed' })
  }

  const { message } = req.body

  const response = await fetch('https://api.deepseek.com/v1/chat/completions', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${process.env.DEEPSEEK_API_KEY}`,
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({
      model: 'deepseek-chat',
      messages: [{ role: 'user', content: message }],
      stream: true,
    }),
  })

  // 流式转发
  res.setHeader('Content-Type', 'text/event-stream')
  res.setHeader('Cache-Control', 'no-cache')
  res.setHeader('Connection', 'keep-alive')

  const reader = response.body?.getReader()
  if (!reader) {
    return res.status(500).json({ error: 'No response body' })
  }

  while (true) {
    const { done, value } = await reader.read()
    if (done) break
    res.write(value)
  }

  res.end()
}
```

### Edge Middleware（请求拦截）

```typescript
// middleware.ts
import { NextResponse } from 'next/server'
import type { NextRequest } from 'next/server'

export function middleware(request: NextRequest) {
  // 国家/地区限制
  const country = request.geo?.country || 'US'
  if (country === 'CN') {
    return NextResponse.redirect(new URL('/cn', request.url))
  }

  // A/B 测试
  const cookie = request.cookies.get('ab-test')
  const variant = cookie?.value || (Math.random() > 0.5 ? 'a' : 'b')

  const response = NextResponse.next()
  response.cookies.set('ab-test', variant)

  if (variant === 'b') {
    request.headers.set('x-variant', 'b')
  }

  return response
}

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
}
```

## 三、Vercel + 自定义域名

```bash
# 添加域名
vercel domains add example.com

# DNS 设置
# A 记录 → 76.76.21.21
# 或 CNAME → cname.vercel-dns.com
```

### 自动 HTTPS

Vercel 自动为所有域名配置 Let's Encrypt SSL 证书，无需手动操作。

## 四、其他云平台对比

### 4.1 平台选型

| 平台 | 类型 | 优势 | 劣势 | 适用场景 |
|------|------|------|------|----------|
| **Vercel** | 全托管 | 前端体验最佳、自动 CDN | 函数执行时间限制 | 前端 + 轻量 API |
| **Netlify** | 全托管 | 表单处理、身份验证 | 构建时间限制更严 | 静态网站、JAMstack |
| **Cloudflare Pages** | 全托管 | 全球边缘节点、免费额度大 | 生态较小 | 全球化应用 |
| **Railway** | PaaS | 简单部署、自动扩缩容 | 费用增长快 | 全栈应用 |
| **Render** | PaaS | 免费 PostgreSQL、简单 | 冷启动慢 | 全栈 + 数据库 |
| **AWS Amplify** | 全托管 | AWS 生态集成 | 配置复杂 | 企业级 AWS 用户 |

### 4.2 Cloudflare Pages 部署

```yaml
# .github/workflows/cloudflare.yml
name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - run: npm ci
      - run: npm run build

      - name: Publish to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: my-project
          directory: dist
```

### 4.3 Railway 部署

```yaml
# railway.yaml
build:
  builder: NIXPACKS

deploy:
  startCommand: npm start
  healthcheckPath: /health
  healthcheckTimeout: 100
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 3
```

```dockerfile
# 备选 Dockerfile 方式
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
EXPOSE 3000
CMD ["node", "index.js"]
```

## 五、监控与告警

### Vercel Analytics

```typescript
// Next.js 内置
import { Analytics } from '@vercel/analytics/react'

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
```

### 自定义监控

```typescript
// lib/monitoring.ts
export async function trackEvent(name: string, data?: Record<string, any>) {
  if (process.env.NODE_ENV === 'development') return

  fetch('https://api.monitoring-service.com/track', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      name,
      data,
      timestamp: Date.now(),
      url: typeof window !== 'undefined' ? window.location.href : undefined,
      userAgent: typeof window !== 'undefined' ? navigator.userAgent : undefined,
    }),
  }).catch(() => {}) // 静默失败，不影响业务
}

// 页面性能监控
export function observeWebVitals() {
  if ('web-vitals' in window) {
    import('web-vitals').then(({ getCLS, getFID, getFCP, getLCP, getTTFB }) => {
      getCLS(console.log)
      getFID(console.log)
      getFCP(console.log)
      getLCP(console.log)
      getTTFB(console.log)
    })
  }
}
```
