# 全局搜索

配置文件如下

- 配置搜索
- 配置翻译
- 配置主题切换的文案

```ts
import { defineConfig } from "vitepress";
import { fileURLToPath } from "node:url";

// https://vitepress.dev/reference/site-config
export default defineConfig({
    // 禁用主题切换  false 表示禁用， 默认是启用的
    appearance: false,
    themeConfig: {
        docFooter: {
            prev: "上一页", // 将 previous page 翻译为上一页
            next: "下一页" // 将 next page 翻译为下一页
        },
        lightModeSwitchTitle: "切换到浅色主题",
        darkModeSwitchTitle: "切换到深色主题",
        search: {
            provider: "local",
            options: {
                translations: {
                    button: {
                        buttonText: "搜索文档",
                        buttonAriaLabel: "搜索文档"
                    },
                    modal: {
                        noResultsText: "无法找到相关结果",
                        resetButtonTitle: "清除查询条件",

                        footer: {
                            selectText: "选择",
                            navigateText: "切换",
                            closeText: "按 ESC 键关闭"
                        },
                        displayDetails: "显示详细信息"
                    }
                }
            }
        },
        outlineTitle: " ",
        outline: "deep",
        // https://vitepress.dev/reference/default-theme-config
        sidebar: [
            // ...
        ],

        // 页脚配置
        footer: {
            message: `版权所有 © ${new Date().getFullYear()} 志豪链云`,
            copyright: "All Rights Reserved. 鄂B2-20240502"
        }
    },

    // 打包输出目录， 默认是 .vitepress/dist
    outDir: fileURLToPath(new URL("../dist", import.meta.url))
});

```

- [效果](https://dsly.ganghaoyun.com/guide/)