# WebGL 实战案例

> 从基础到进阶的 WebGL 实战案例，涵盖常见 3D 渲染场景。

---

## 目录

1. [案例一：绘制旋转立方体](#案例一绘制旋转立方体)
2. [案例二：带光照的材质球](#案例二带光照的材质球)
3. [案例三：加载纹理贴图](#案例三加载纹理贴图)
4. [案例四：阴影渲染](#案例四阴影渲染)
5. [案例五：后处理模糊效果](#案例五后处理模糊效果)

---

## 案例一：绘制旋转立方体

### 完整代码

```html
<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>WebGL 旋转立方体</title>
  <style>
    body { margin: 0; }
    canvas { display: block; width: 100vw; height: 100vh; }
  </style>
</head>
<body>
<canvas id="canvas"></canvas>
<script type="module">
import { mat4 } from 'https://cdn.skypack.dev/gl-matrix';

const canvas = document.getElementById('canvas');
const gl = canvas.getContext('webgl2');
canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

// 顶点着色器
const vsSource = `#version 300 es
  in vec3 aPosition;
  in vec3 aColor;
  uniform mat4 uMVP;
  out vec3 vColor;
  void main() {
    gl_Position = uMVP * vec4(aPosition, 1.0);
    vColor = aColor;
  }
`;

// 片元着色器
const fsSource = `#version 300 es
  precision highp float;
  in vec3 vColor;
  out vec4 fragColor;
  void main() {
    fragColor = vec4(vColor, 1.0);
  }
`;

// 编译着色器
function createShader(gl, type, source) {
  const shader = gl.createShader(type);
  gl.shaderSource(shader, source);
  gl.compileShader(shader);
  return shader;
}

const program = gl.createProgram();
gl.attachShader(program, createShader(gl, gl.VERTEX_SHADER, vsSource));
gl.attachShader(program, createShader(gl, gl.FRAGMENT_SHADER, fsSource));
gl.linkProgram(program);
gl.useProgram(program);

// 立方体顶点（位置和颜色）
const vertices = new Float32Array([
  // 前面 (z = 0.5)           // 颜色
  -0.5, -0.5,  0.5,  1.0, 0.0, 0.0,
   0.5, -0.5,  0.5,  1.0, 0.0, 0.0,
   0.5,  0.5,  0.5,  1.0, 0.0, 0.0,
  -0.5,  0.5,  0.5,  1.0, 0.0, 0.0,
  // 后面 (z = -0.5)
  -0.5, -0.5, -0.5,  0.0, 1.0, 0.0,
   0.5, -0.5, -0.5,  0.0, 1.0, 0.0,
   0.5,  0.5, -0.5,  0.0, 1.0, 0.0,
  -0.5,  0.5, -0.5,  0.0, 1.0, 0.0,
]);

const indices = new Uint16Array([
  0,1,2, 0,2,3,   // 前
  5,4,7, 5,7,6,   // 后
  4,0,3, 4,3,7,   // 左
  1,5,6, 1,6,2,   // 右
  3,2,6, 3,6,7,   // 上
  4,5,1, 4,1,0,   // 下
]);

// 创建 VAO (WebGL 2.0)
const vao = gl.createVertexArray();
gl.bindVertexArray(vao);

const vbo = gl.createBuffer();
gl.bindBuffer(gl.ARRAY_BUFFER, vbo);
gl.bufferData(gl.ARRAY_BUFFER, vertices, gl.STATIC_DRAW);

const posLoc = gl.getAttribLocation(program, 'aPosition');
gl.enableVertexAttribArray(posLoc);
gl.vertexAttribPointer(posLoc, 3, gl.FLOAT, false, 24, 0);

const colorLoc = gl.getAttribLocation(program, 'aColor');
gl.enableVertexAttribArray(colorLoc);
gl.vertexAttribPointer(colorLoc, 3, gl.FLOAT, false, 24, 12);

const ebo = gl.createBuffer();
gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER, ebo);
gl.bufferData(gl.ELEMENT_ARRAY_BUFFER, indices, gl.STATIC_DRAW);

// 矩阵
const model = mat4.create();
const view = mat4.create();
const proj = mat4.create();
const mvp = mat4.create();

mat4.lookAt(view, [2, 2, 3], [0, 0, 0], [0, 1, 0]);
mat4.perspective(proj, Math.PI / 4, canvas.width / canvas.height, 0.1, 100.0);

const uMVPLoc = gl.getUniformLocation(program, 'uMVP');

// 渲染循环
let angle = 0;
function render() {
  angle += 0.01;
  mat4.identity(model);
  mat4.rotateY(model, model, angle);
  mat4.rotateX(model, model, angle * 0.5);

  mat4.multiply(mvp, proj, view);
  mat4.multiply(mvp, mvp, model);

  gl.viewport(0, 0, canvas.width, canvas.height);
  gl.clearColor(0.1, 0.1, 0.15, 1.0);
  gl.clear(gl.COLOR_BUFFER_BIT | gl.DEPTH_BUFFER_BIT);
  gl.enable(gl.DEPTH_TEST);

  gl.uniformMatrix4fv(uMVPLoc, false, mvp);
  gl.bindVertexArray(vao);
  gl.drawElements(gl.TRIANGLES, indices.length, gl.UNSIGNED_SHORT, 0);

  requestAnimationFrame(render);
}
render();
</script>
</body>
</html>
```

### 要点解析

1. **VAO（Vertex Array Object）**：WebGL 2.0 中一次性绑定顶点配置
2. **索引绘制**：`drawElements` 复用顶点，减少数据量
3. **MVP 矩阵**：每帧更新模型旋转，投影/视图保持不变

---

## 案例二：带光照的材质球

### Phong 光照模型

```glsl
// 片元着色器
#version 300 es
precision highp float;

in vec3 vNormal;
in vec3 vPosition;

uniform vec3 uLightPos;
uniform vec3 uCameraPos;
uniform vec3 uDiffuseColor;
uniform vec3 uSpecularColor;
uniform float uShininess;

out vec4 fragColor;

void main() {
  vec3 normal = normalize(vNormal);
  vec3 lightDir = normalize(uLightPos - vPosition);
  vec3 viewDir = normalize(uCameraPos - vPosition);
  vec3 halfDir = normalize(lightDir + viewDir);

  // 漫反射
  float diff = max(dot(normal, lightDir), 0.0);
  vec3 diffuse = diff * uDiffuseColor;

  // 镜面反射 (Blinn-Phong)
  float spec = pow(max(dot(normal, halfDir), 0.0), uShininess);
  vec3 specular = spec * uSpecularColor;

  // 环境光
  vec3 ambient = 0.1 * uDiffuseColor;

  fragColor = vec4(ambient + diffuse + specular, 1.0);
}
```

---

## 案例三：加载纹理贴图

```javascript
async function loadTexture(gl, url) {
  const texture = gl.createTexture();
  gl.bindTexture(gl.TEXTURE_2D, texture);

  // 占位像素（加载完成前显示）
  gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, 1, 1, 0, gl.RGBA, gl.UNSIGNED_BYTE,
    new Uint8Array([128, 128, 128, 255]));

  const image = new Image();
  image.src = url;
  await new Promise(r => image.onload = r);

  gl.bindTexture(gl.TEXTURE_2D, texture);
  gl.texImage2D(gl.TEXTURE_2D, 0, gl.RGBA, gl.RGBA, gl.UNSIGNED_BYTE, image);
  gl.generateMipmap(gl.TEXTURE_2D);

  return texture;
}
```

---

## 案例四：阴影渲染

阴影映射（Shadow Mapping）是实时渲染中最常用的阴影技术：

```
第一遍：从光源视角渲染深度图 → 存储到深度纹理
第二遍：正常渲染，将片元坐标转换到光源空间，与深度图比较
```

```glsl
// 第二遍片元着色器
uniform sampler2D uShadowMap;
in vec4 vLightSpacePos;

float shadowCalculation() {
  vec3 projCoords = vLightSpacePos.xyz / vLightSpacePos.w;
  projCoords = projCoords * 0.5 + 0.5;

  float closestDepth = texture(uShadowMap, projCoords.xy).r;
  float currentDepth = projCoords.z;

  float bias = 0.005;
  return currentDepth - bias > closestDepth ? 1.0 : 0.0;
}
```

---

## 案例五：后处理模糊效果

```glsl
// 高斯模糊片元着色器
#version 300 es
precision highp float;

uniform sampler2D uTexture;
uniform vec2 uDirection;
uniform vec2 uResolution;

in vec2 vTexCoord;
out vec4 fragColor;

const float weights[5] = float[](0.227, 0.194, 0.121, 0.054, 0.016);

void main() {
  vec2 texOffset = uDirection / uResolution;
  vec3 result = texture(uTexture, vTexCoord).rgb * weights[0];

  for (int i = 1; i < 5; i++) {
    result += texture(uTexture, vTexCoord + texOffset * float(i)).rgb * weights[i];
    result += texture(uTexture, vTexCoord - texOffset * float(i)).rgb * weights[i];
  }

  fragColor = vec4(result, 1.0);
}
```

### 两遍模糊

高斯模糊可分离为水平 + 垂直两遍，将 O(n²) 降为 O(2n)：

```javascript
// 第一遍：水平模糊
gl.uniform2f(uDirectionLoc, 1, 0);
renderToFBO(horizontalFBO, sceneTexture);

// 第二遍：垂直模糊
gl.uniform2f(uDirectionLoc, 0, 1);
renderToScreen(horizontalFBO.texture);
```
